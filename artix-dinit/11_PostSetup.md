# Post Setup

> Assuming you are currently booted into the system

Enable the services

## Services

Enable NetworkManager, iwd, & zramen:

```bash
sudo dinitctl enable NetworkManager
sudo dinitctl enable iwd
sudo dinitctl enable zramen
```
